
function getMainPokedexTemplate(i) {
    return `
    <div>
        <h3 class="normal"></h3>
        <p>Moves:</p>
        <ul>
           
        </ul>
    </div>`;
};
 // ${pokemonBatch}

//  ${results[i].name}
